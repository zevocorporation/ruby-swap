import React from "react";

const Header = () => {
  return (
    <div className="header">
      <img className="logo" />

      <div className="menu">
        <a id="home">Home</a>
      </div>
      <button>connect</button>
    </div>
  );
};

export default Header;
