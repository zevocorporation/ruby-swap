import React from "react";

//IMPORTING STYLESHEET

import "../styles/screens/predictionScreen.scss";

const PredictionScreen = () => {
  const renderScreen = (
    <div className="prediction_screen">PredictionScreen</div>
  );
  return <>{renderScreen}</>;
};

export default PredictionScreen;
